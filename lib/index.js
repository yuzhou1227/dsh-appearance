// dsh-appearance — client-only plugin: the DSH Web GUI host half has nothing to do.
// Kept as a valid minimal Cordis host plugin so the bundle loads cleanly.
export const name = "dsh-appearance";
export const inject = [];
export function apply() {
  // client-only: see ./client.js
}
